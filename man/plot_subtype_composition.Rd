% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_annotation.R
\name{plot_subtype_composition}
\alias{plot_subtype_composition}
\title{Stacked bar of mutually-exclusive subtype composition within one parent type}
\usage{
plot_subtype_composition(
  subtype_summary,
  parent,
  group = "Sample",
  percent = FALSE
)
}
\arguments{
\item{subtype_summary}{The \code{long} tibble from \code{\link[=summarize_subtypes]{summarize_subtypes()}} (or that
list), with columns \code{CellType}, \code{Subtype}, \code{nCells}, and the grouping column.}

\item{parent}{The parent \code{CellType} to plot (e.g. "CD8 T cell").}

\item{group}{Name of the grouping column to put on the axis (default
"Sample"). The summary must already be aggregated to this grouping.}

\item{percent}{If TRUE, bars are scaled to 100\% within each group.}
}
\value{
A ggplot object, or \code{NULL} if the parent has no cells.
}
\description{
Shows, per group (sample, TMA block, ...), how the cells of a single parent
lineage split across its subtypes. Subtypes are mutually exclusive, so the
bars partition the parent's cells. The \code{(NA / un-callable)} bucket is greyed.
}
