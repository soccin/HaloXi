% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotate_cells.R
\name{state_tags}
\alias{state_tags}
\title{Column-name tags for the state parents of a rules file}
\usage{
state_tags(rules)
}
\arguments{
\item{rules}{Parsed rules from \code{\link[=read_cell_rules]{read_cell_rules()}}.}
}
\value{
A named character vector, one entry per parent in \code{rules$states}:
names are the parent lineages, values their tags. Empty if the rules define
no states.
}
\description{
State flag columns are named \verb{<tag>_<state>}. This returns the tag every
parent in \code{rules$states} resolves to, so a caller can name per-parent outputs
(files, plots) the same way the columns are named.
}
