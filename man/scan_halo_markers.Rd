% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scan_manifest.R
\name{scan_halo_markers}
\alias{scan_halo_markers}
\title{Scan Halo file headers for marker presence (no cell loading)}
\usage{
scan_halo_markers(manifest)
}
\arguments{
\item{manifest}{A validated manifest tibble from \code{\link[=read_manifest]{read_manifest()}}.}
}
\value{
A long tibble with columns \code{Sample}, \code{Marker}, \code{MarkerNorm},
\code{Present} (always TRUE for rows that appear). Markers absent from a given
sample simply do not appear; use \code{\link[=marker_presence_matrix]{marker_presence_matrix()}} to get the
full rectangular matrix.
}
\description{
Reads only the header line of each Halo CSV and extracts the marker panel
by applying the same column-name normalisation \code{load_halo()} uses, then
selecting \verb{*_Positive_Classification} columns. Fast even for very large
files because no cell rows are read.
}
