% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_annotation.R
\name{plot_parent_states}
\alias{plot_parent_states}
\title{Barplot of one parent lineage's state flags}
\usage{
plot_parent_states(state_summary, tag, parent_label = tag)
}
\arguments{
\item{state_summary}{Long tibble from \code{\link[=summarize_states]{summarize_states()}}.}

\item{tag}{The parent's state-column tag, as returned by \code{\link[=state_tags]{state_tags()}}.
Rows whose \code{State} begins \verb{<tag>_} are shown, matched as a fixed prefix so
a tag can never pick up a longer tag's rows.}

\item{parent_label}{Display name for the parent in the title and axis
(defaults to \code{tag}).}
}
\value{
A ggplot object, or \code{NULL} if no state rows carry that tag.
}
\description{
Focused view of a single parent type: of that type's cells in each sample,
how many carry each of its state flags (independent flags, not a partition).
}
