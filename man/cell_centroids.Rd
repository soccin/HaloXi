% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial_analysis.R
\name{cell_centroids}
\alias{cell_centroids}
\title{Cell centroids in microns}
\usage{
cell_centroids(obj, um_per_px)
}
\arguments{
\item{obj}{An annotated object from \code{\link[=annotate_cells]{annotate_cells()}}.}

\item{um_per_px}{Microns per pixel. \strong{Required, with no default.}}
}
\value{
A tibble of \code{UUID}, \code{Sample}, \code{CellType} (factor, unused levels
dropped), \code{X}, \code{Y}, with \code{X}/\code{Y} in microns.
}
\description{
Halo exports a bounding box per cell, in pixels, and carries no scan
metadata. The centroid is the midpoint of that box, converted to microns.
}
\details{
Cells whose \code{CellType} is \code{NA} -- un-callable, because a marker the call
needed was not run in that sample -- are dropped: they cannot take part in a
type-based test, and carrying them into the permutation would invent a label
for them. Cells labelled as the rules' conflict (\code{UNKNOWN}) or no-lineage
(\code{UNCLASSIFIED}) buckets are \strong{kept}. They are real cells occupying real
space, and dropping them would distort both the local density and the
marginal type frequencies the null is built from. They appear in results as
categories of their own.
}
