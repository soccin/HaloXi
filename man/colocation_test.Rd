% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial_analysis.R
\name{colocation_test}
\alias{colocation_test}
\title{Which cell types sit closer together than chance}
\usage{
colocation_test(
  obj,
  um_per_px,
  r_outer = 50,
  r_inner = 0,
  n_perm = 99,
  seed = 1,
  verbose = TRUE
)
}
\arguments{
\item{obj}{An annotated object from \code{\link[=annotate_cells]{annotate_cells()}}.}

\item{um_per_px}{Microns per pixel. \strong{Required, with no default.}}

\item{r_outer, r_inner}{The distance band, in microns: \verb{r_inner < d <= r_outer}.}

\item{n_perm}{Label permutations used to build the null.}

\item{seed}{RNG seed, so a result is reproducible.}

\item{verbose}{Progress messages.}
}
\value{
A tibble, one row per sample x type-pair, with \code{RadiusInner},
\code{RadiusOuter}, \code{Sample}, \code{TypeA}, \code{TypeB} (\code{TypeA} <= \code{TypeB} in level
order), \code{nA}, \code{nB}, \code{nObs}, \code{nExp}, \code{sdExp}, \code{log2FC}, \code{z} (\code{NA} where
\code{sdExp} is 0), \code{pEmp} and \code{qBH} (Benjamini-Hochberg within sample).
}
\description{
For every unordered pair of cell types, counts the cell pairs of those types
whose centres fall in the band \verb{r_inner < d <= r_outer}, and compares that
count against a null built by permuting the cell-type labels across the same
point pattern, within sample. Self-pairs (a type with itself) are included
and measure how clustered that type is.
}
\details{
Samples are analysed separately throughout. They are different tissues with
different composition and geometry, and pooling them would let the largest
specimen decide every call.
}
\section{The p-value floor}{

\code{pEmp} can never fall below \code{2 / (n_perm + 1)} -- with the default 99
permutations, 0.02. A row sitting at that floor means "smaller than this
test can resolve", not "p = 0". Raise \code{n_perm} to resolve further; never
report such a row as \code{p = 0}.
}

\section{Edge effects}{

Cells near the tissue border have truncated neighbourhoods, so raw pair
\strong{counts} are biased low there. \code{log2FC} is robust to it: the null is
computed over the same point pattern with the same truncation, so the bias
hits \code{nObs} and \code{nExp} identically and cancels in the ratio. No border
correction or guard band is applied, deliberately -- lead with \code{log2FC}
rather than with \code{nObs}.
}

