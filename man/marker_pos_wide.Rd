% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotate_cells.R
\name{marker_pos_wide}
\alias{marker_pos_wide}
\title{Pivot marker.data to one logical column per marker, per cell}
\usage{
marker_pos_wide(obj, rules)
}
\arguments{
\item{obj}{A combined object from \code{\link[=load_manifest]{load_manifest()}}.}

\item{rules}{Parsed rules from \code{\link[=read_cell_rules]{read_cell_rules()}}.}
}
\value{
A tibble with \code{UUID}, \code{Sample}, and one logical column per friendly
marker name. \code{TRUE}/\code{FALSE} where scored, \code{NA} where the marker is absent
from that cell's sample panel.
}
\description{
Builds a wide cell x marker table of positivity from the combined object.
Crucially, a marker that is \strong{absent from a sample's panel} is \code{NA} for that
sample's cells (not \code{FALSE}): un-callable, never silently negative. This is
what makes downstream calls collapse to \code{NA} rather than a wrong answer on
partial panels.
}
\details{
Columns are named by the friendly marker name from the rules (e.g. \code{CD30}),
resolved from the data's \code{MarkerNorm} via the rules' \verb{markers:} map. Data
markers not present in the rules are dropped (with the exception that nothing
breaks if they are missing).
}
