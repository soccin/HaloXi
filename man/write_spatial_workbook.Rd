% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_spatial_workbooks.R
\name{write_spatial_workbook}
\alias{write_spatial_workbook}
\title{Write the spatial-analysis Excel workbook}
\usage{
write_spatial_workbook(coloc, neigh, outdir)
}
\arguments{
\item{coloc}{The tibble from \code{\link[=colocation_test]{colocation_test()}}, for one or several bands.}

\item{neigh}{The list from \code{\link[=neighborhood_composition]{neighborhood_composition()}}.}

\item{outdir}{Directory to write the workbook into (created if needed).}
}
\value{
The workbook path written (invisibly).
}
\description{
One collaborator workbook (\code{Spatial_analysis.xlsx}) with the co-location
test, the neighbourhood composition, and the spread of ring sizes behind the
neighbourhood means.
}
\details{
The per-anchor table itself is deliberately not a sheet: it carries one row
per anchor cell, which on a whole-section specimen is hundreds of thousands
of rows that no one reads in Excel. Its distribution is summarised here and
the full table is written as a CSV by the driver.
}
