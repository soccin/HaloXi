% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scan_manifest.R
\name{resolve_cache_path}
\alias{resolve_cache_path}
\title{Default cache location for a driver's output directory}
\usage{
resolve_cache_path(outdir, cache_arg = NULL)
}
\arguments{
\item{outdir}{The driver's output directory.}

\item{cache_arg}{The value of the driver's \verb{--cache=} argument, or NULL when
it was not given. A non-empty value is returned unchanged.}
}
\value{
A path to the RDS cache file. Nothing is created; \code{\link[=load_manifest]{load_manifest()}}
creates the directory when it writes.
}
\description{
The loaded object is a build artifact of ~600 MB on a whole-section study,
and \code{outdir} is a deliverable -- the folder that gets zipped and sent. So the
cache is never written inside \code{outdir}: it goes to \verb{cache/<name>/} relative
to the working directory, where \verb{<name>} is the last component of \code{outdir}.
}
\details{
Keeping \code{outdir}'s own name, rather than sharing one cache across every
stage, is deliberate. The QC scan's default row cap is 100 and an analysis
run is \code{--full}, so a shared path would let a quick scan discard a full cache
and cost a re-read of the raw CSVs. Two runs of the same stage \emph{do} share, and
correctly: the loaded object depends on the manifest and the row cap and on
nothing else.
}
