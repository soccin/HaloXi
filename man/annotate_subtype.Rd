% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotate_cells.R
\name{annotate_subtype}
\alias{annotate_subtype}
\title{Assign a mutually-exclusive sub-type within a resolved cell type}
\usage{
annotate_subtype(wide, cell_type, rules)
}
\arguments{
\item{wide}{Wide cell x marker logical table from \code{\link[=marker_pos_wide]{marker_pos_wide()}}.}

\item{cell_type}{Character vector of resolved cell types (from
\code{\link[=annotate_lineage]{annotate_lineage()}}), aligned row-wise to \code{wide}.}

\item{rules}{Parsed rules from \code{\link[=read_cell_rules]{read_cell_rules()}}.}
}
\value{
A character vector of subtype names, one per row of \code{wide}; \code{NA}
where un-callable or the cell's parent has no subtypes.
}
\description{
For each parent lineage that has a \verb{subtypes:} block, evaluates the parent's
ordered list of \verb{\{name, require_pos, require_neg\}} rules with three-valued
logic and assigns the \strong{first definitely-matching} rule's name. This is the
hierarchical layer the flat \code{lineages}/\code{states} model cannot express: within
an already-resolved parent, a cell gets exactly one subtype.
}
\details{
Resolution per cell of a parent with subtypes:
\itemize{
\item the first rule whose \code{require_pos}/\code{require_neg} definitely hold -> its name;
\item no rule definitely holds but at least one is un-callable (a needed
marker absent from the sample) -> \code{NA} (the lineage call is unaffected);
\item no rule holds and all are callable -> the parent's \code{fallthrough} name
if given, else \code{NA} (an intentionally incomplete partition).
}
Cells whose \code{cell_type} is not a parent with subtypes (including UNKNOWN /
UNCLASSIFIED / NA, and lineages without a \verb{subtypes:} block) get \code{NA}.
}
