% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial_analysis.R
\name{neighborhood_composition}
\alias{neighborhood_composition}
\title{What the tissue in a ring around one cell type is made of}
\usage{
neighborhood_composition(
  obj,
  um_per_px,
  anchor_type = "Tumor",
  r_inner = 20,
  r_outer = 50,
  n_perm = 99,
  seed = 1,
  verbose = TRUE
)
}
\arguments{
\item{obj}{An annotated object from \code{\link[=annotate_cells]{annotate_cells()}}.}

\item{um_per_px}{Microns per pixel. \strong{Required, with no default.}}

\item{anchor_type}{The cell type to measure outward from.}

\item{r_inner, r_outer}{The ring, in microns: \verb{r_inner < d <= r_outer}.}

\item{n_perm}{Label permutations used to build the null.}

\item{seed}{RNG seed, so a result is reproducible.}

\item{verbose}{Progress messages.}
}
\value{
A list of \code{summary} (one row per sample x neighbour type, with
\code{RadiusInner}, \code{RadiusOuter}, \code{nAnchors}, \code{nNeighbors}, \code{meanPerAnchor},
\code{pctOfRing}, \code{pctBackground}, \code{nExp}, \code{log2Enrich}, \code{z}, \code{pEmp}, \code{qBH}),
\code{per_anchor} (one row per anchor cell: \code{UUID}, \code{Sample}, a count column per
neighbour type, and \code{nRing}), and \code{params} (every argument, echoed back).
}
\description{
For every cell of \code{anchor_type}, tabulates the types of all cells whose
centres fall in the ring \verb{r_inner < d <= r_outer} around it. A ring, not a
disc: it describes the surrounding tissue rather than the cells in immediate
contact.
}
\details{
The anchor's own type is included among the neighbour types. Anchor-anchor
neighbours measure how clustered the anchor population is, which is a result
in its own right, not a self-count to be removed.
}
\section{The null}{

The anchor set is fixed from the observed labels. Each permutation reassigns
the cell-type labels across the sample's points and recounts the neighbour
types over the \strong{same} anchors and the \strong{same} rings. Anchors are not
recomputed from the permuted labels: that would move the anchor population
around the tissue, which measures something else and destroys the edge-effect
cancellation below.
}

\section{Edge effects}{

Anchors near the tissue border have truncated rings, so \code{nNeighbors} and
\code{meanPerAnchor} are biased low. \code{log2Enrich} is robust to it -- the null is
computed on the same anchors with the same truncated rings, so the truncation
hits observed and expected identically and cancels in the ratio. No border
correction or guard band is applied, deliberately; lead with \code{pctOfRing} and
\code{log2Enrich} rather than with counts.
}

\section{The p-value floor}{

As for \code{\link[=colocation_test]{colocation_test()}}, \code{pEmp} cannot fall below \code{2 / (n_perm + 1)}.
}

