% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scan_manifest.R
\name{scan_manifest}
\alias{scan_manifest}
\title{Run the initial QC scan of a Halo sample manifest}
\usage{
scan_manifest(
  manifest_csv,
  cache_rds = NULL,
  refresh = FALSE,
  controlMarkers = c("DAPI"),
  n_max = 100,
  max_points_per_sample = 30000
)
}
\arguments{
\item{manifest_csv}{Path to the manifest CSV.}

\item{cache_rds}{Optional RDS cache path for the combined loaded object.}

\item{refresh}{If TRUE, ignore an existing cache.}

\item{controlMarkers}{Markers treated as controls.}

\item{n_max}{Max data rows to read per Halo file (default 100 for fast QC;
\code{Inf} for a full load).}

\item{max_points_per_sample}{Down-sample cap for the spatial footprint plot.}
}
\value{
A list with \code{tables} (named list of summary tibbles), \code{plots}
(named list of ggplot objects), \code{obj} (the combined loaded object), and
\code{meta} (scan metadata: timestamp, version, counts, n_max).
}
\description{
Convenience wrapper that reads the manifest, scans marker panels from
headers, loads samples via \code{\link[=load_halo]{load_halo()}} (cached), and assembles every
summary table and plot. The CLI driver (\code{scripts/scan_data.R}) calls
this and then writes the Excel workbooks, PNGs and HTML report.
}
\details{
By default only the first \code{n_max} rows of each Halo file are read, giving a
fast initial QC pass. Marker-panel coverage (presence) is always derived
from the full headers and so is unaffected by \code{n_max}; only the cell counts
and positivity percentages reflect the sampled rows. Set \code{n_max = Inf} for a
complete load.
}
