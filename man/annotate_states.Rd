% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotate_cells.R
\name{annotate_states}
\alias{annotate_states}
\title{Compute parent-gated sub-state flags for every cell}
\usage{
annotate_states(wide, cell_type, rules)
}
\arguments{
\item{wide}{Wide cell x marker logical table from \code{\link[=marker_pos_wide]{marker_pos_wide()}}.}

\item{cell_type}{Character vector of resolved cell types (from
\code{\link[=annotate_lineage]{annotate_lineage()}}), aligned row-wise to \code{wide}.}

\item{rules}{Parsed rules from \code{\link[=read_cell_rules]{read_cell_rules()}}.}
}
\value{
A tibble of state columns (one per state across all parents, plus
\code{Exhausted}), aligned row-wise to \code{wide}. Column names are prefixed by a
short parent tag, e.g. \code{Tumor_Ki67_pos}, \code{T_CD4}, \code{Mac_M1}.
}
\description{
For each parent lineage in \code{rules$states}, sets each state's boolean flag
only on cells whose \code{CellType} equals that parent; all other cells get \code{NA}.
Adds the \code{Exhausted} flag (a state on T/NK cells, positive for TIM3 or LAG3).
Where a state's marker is absent from a cell's sample panel the flag is \code{NA},
even for cells of the right parent type.
}
