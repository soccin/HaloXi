% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotate_cells.R
\name{summarize_states}
\alias{summarize_states}
\title{Sub-state breakdowns (tumor flags, T subsets, M1/M2, exhaustion)}
\usage{
summarize_states(obj)
}
\arguments{
\item{obj}{An annotated object from \code{\link[=annotate_cells]{annotate_cells()}}.}
}
\value{
A long tibble: \code{Sample}, \code{State}, \code{nScored} (parent cells with a
callable flag = the denominator), \code{nPos} (flag TRUE), \code{pctPos} (of
\code{nScored}). A state with \code{nScored == 0} for a sample (e.g. Th1/Th2/Th17 in
S3, where the marker is absent) reports \code{pctPos = NA}, never a false 0.
}
\description{
Counts, per sample, how many cells of the relevant parent type carry each
state flag. A flag value of \code{NA} (marker absent from the sample's panel, or
the cell is not of the parent type) is reported separately so a small
denominator from a missing marker is never mistaken for a real zero.
}
