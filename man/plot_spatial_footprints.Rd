% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scan_manifest.R
\name{plot_spatial_footprints}
\alias{plot_spatial_footprints}
\title{Per-sample spatial footprint plots}
\usage{
plot_spatial_footprints(
  obj,
  max_points_per_sample = 30000,
  point_alpha = 0.4,
  seed = 101
)
}
\arguments{
\item{obj}{A combined object from \code{\link[=load_manifest]{load_manifest()}}.}

\item{max_points_per_sample}{Down-sample each sample to at most this many
points so the plot stays light for very large samples.}

\item{point_alpha}{Point transparency (0.3-0.5 reads well for dense tissue).}

\item{seed}{RNG seed for reproducible down-sampling.}
}
\value{
A named list of ggplot objects, one per sample (names are sample ids).
}
\description{
Produces one clean, standalone spatial-footprint plot per sample (cell
centroids coloured by phenotyped status). These are the key QC plots, so
each sample gets its own full-resolution figure rather than a shared facet.
}
