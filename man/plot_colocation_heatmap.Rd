% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_spatial.R
\name{plot_colocation_heatmap}
\alias{plot_colocation_heatmap}
\title{Heatmap of which cell-type pairs sit closer together than chance}
\usage{
plot_colocation_heatmap(coloc, sig_q = 0.05)
}
\arguments{
\item{coloc}{The tibble from \code{\link[=colocation_test]{colocation_test()}}.}

\item{sig_q}{Significance cut on \code{qBH}. Cells above it are muted, not hidden
-- an absent tile and a non-significant one are different statements.}
}
\value{
A ggplot object.
}
\description{
Cell type against cell type, filled by \code{log2FC} -- how many more (or fewer)
pairs of those two types fall in the distance band than a random arrangement
of the same labels over the same cells would give. Pairs that do not clear
\code{sig_q} are drawn muted, so the eye lands on the ones the data supports.
}
\details{
The matrix is mirrored for reading: \code{\link[=colocation_test]{colocation_test()}} returns each
unordered pair once, and both triangles are drawn from that single row.
}
