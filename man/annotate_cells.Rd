% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotate_cells.R
\name{annotate_cells}
\alias{annotate_cells}
\title{Annotate a loaded Halo object with cell types and states}
\usage{
annotate_cells(obj, rules)
}
\arguments{
\item{obj}{A combined object from \code{\link[=load_manifest]{load_manifest()}}.}

\item{rules}{Parsed rules from \code{\link[=read_cell_rules]{read_cell_rules()}} (required).}
}
\value{
The input \code{obj} with \code{cell.data} extended by \code{CellType} (factor,
levels = lineages + UNKNOWN + UNCLASSIFIED), the per-state flag columns,
\code{Exhausted}, and \code{CellState} (a \verb{;}-joined string of the cell's TRUE state
flags). Adds \code{rules_path} and \code{VERSION} markers to the object.
}
\description{
One-call entry point: builds the wide positivity table, assigns each cell a
\code{CellType}, computes parent-gated state flags and \code{Exhausted}, and a compact
\code{CellState} summary string, then returns the object with these columns added
to \code{cell.data}.
}
